# The installer uses these directly so openWakeWord's unused ONNX/training
# dependencies never enter the constrained Pi runtime.
numpy>=1.23.2,<2
requests>=2.32,<3
sounddevice>=0.5,<1
tflite-runtime>=2.14,<3
tqdm>=4,<5
webrtcvad-wheels>=2.0.14,<3
